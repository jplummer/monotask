// Committed wrapper: optional local file supplies DEVELOPMENT_TEAM (not touched by xcodegen).
// Copy MonotaskerSigning.local.xcconfig.example → MonotaskerSigning.local.xcconfig (see README).
#include? "MonotaskerSigning.local.xcconfig"
